import React from "react";
import EditorContainer from '../EditorContainer';

const Home = () => {
    return (
        <>
            <EditorContainer></EditorContainer>
        </>
    );
};

export default Home;
